const API="http://127.0.0.1:8000"; const $=id=>document.getElementById(id);
async function request(url, options={}){const res=await fetch(url, options); if(!res.ok) throw new Error(await res.text()); return res.json();}
function esc(v){return String(v??'').replace(/[&<>"]/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;'}[c]));}
async function load(){const data=await request(`${API}/api/checklist-template`); const body=$('templateBody'); body.innerHTML=''; for(const item of data.items){const tr=document.createElement('tr'); tr.innerHTML=`<td><input class="form-control form-control-sm" value="${esc(item.phase)}"></td><td><input class="form-control form-control-sm" value="${esc(item.role)}"></td><td><input class="form-control form-control-sm" value="${esc(item.warehouse_type)}"></td><td><textarea class="form-control form-control-sm" rows="2">${esc(item.task)}</textarea></td><td class="text-nowrap"><button class="btn btn-sm btn-outline-primary me-1">Guardar</button><button class="btn btn-sm btn-outline-danger">Eliminar</button></td>`; const inputs=tr.querySelectorAll('input, textarea'); tr.querySelector('.btn-outline-primary').addEventListener('click',async()=>{await request(`${API}/api/checklist-template/${item.id}`,{method:'PATCH',headers:{'Content-Type':'application/json'},body:JSON.stringify({phase:inputs[0].value,role:inputs[1].value,warehouse_type:inputs[2].value,task:inputs[3].value})}); load();}); tr.querySelector('.btn-outline-danger').addEventListener('click',async()=>{if(confirm('¿Eliminar de la plantilla global?')){await request(`${API}/api/checklist-template/${item.id}`,{method:'DELETE'}); load();}}); body.appendChild(tr);}}
document.addEventListener('DOMContentLoaded',()=>{$('addTemplate').addEventListener('click',async()=>{const payload={phase:$('newPhase').value.trim(),role:$('newRole').value.trim(),warehouse_type:$('newWarehouse').value.trim(),task:$('newTask').value.trim()}; if(!payload.phase||!payload.role||!payload.warehouse_type||!payload.task){alert('Completa todos los campos.');return;} await request(`${API}/api/checklist-template`,{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify(payload)}); ['newPhase','newRole','newWarehouse','newTask'].forEach(id=>$(id).value=''); load();}); load().catch(err=>alert(err.message));});
